package au.com.addstar.ccm.api;

/**
 * Format of the message string for channel messages.
 */
public enum MessageFormat {
    /** Legacy colour codes (e.g. &a, §a). */
    LEGACY,
    /** MiniMessage format (e.g. <green>text</green&>). */
    MINIMESSAGE
}
